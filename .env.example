# Base URL of the public order-tracking API (see public_api.md).
# Falls back to the deployed default in src/services/orderTracking.js if unset.
VITE_API_BASE_URL=https://22studio-payroll-system.vercel.app/api/public
